# Live Tests

`agent-server` keeps live validation separate from default automated test targets.

Use this layer for:

- real device sessions
- local worker processes
- cloud-provider-backed ASR/TTS/LLM flows
- archived browser/manual evidence

Canonical references:

- `docs/codex/live-validation-runbook.md`
- `artifacts/live-smoke/`
- `artifacts/live-baseline/`
